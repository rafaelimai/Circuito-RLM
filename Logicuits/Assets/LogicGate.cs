using UnityEngine;
using System.Collections;

public class LogicGate : MonoBehaviour {
	// The Logic Gate propgates the state variable from its inputs onto it's output.
	// The LogicGate.type defines the function it'll employ in order to.

	public string type;


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

		// When Level_setup.verify is engaged, gates will continuously attempt to propagate
		// the state in its inputs to its
	
	}
}
